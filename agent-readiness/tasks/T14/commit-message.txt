test(config): T14 make test profiles explicit

Define explicit CI and factory pilot profiles while preserving the existing
compiler, optimizer, EVM and isolate settings. The local and Ethereum factory
profiles deny FFI and filesystem cheatcodes because every classified pilot
suite records that it needs neither.

Update the suite catalog and test guidance so each pilot invocation selects its
restricted profile explicitly, while the full CI and Arbitrum profiles retain
their previous effective behaviour.
