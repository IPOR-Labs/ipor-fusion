T52 verification — 2026-09-09 (Europe/Warsaw)

Capabilities of the existing server, checked first:
  server_info → ipor-fusion-dev, SDK 3.6.7 (2026-09-08)
  It answers discovery and chain questions: fusion_address_lookup,
  fusion_address_names, contract_abi, contract_source, vault_info,
  decode_fuse_transaction, keeper and market queries.
  It does not serve this repository's deployments/, its verification reports or
  its compatibility tests — the evidence of compatibility added in T21–T26 and
  T49–T51. That gap is what the two new tools fill.

PASS npm run mcp:deployments:test   (with a provider)
  tests 4, pass 4, fail 0
    handshake: protocolVersion 2024-11-05, serverInfo ipor-fusion-deployments
    tools/list is exactly ["inspect_factory", "list_deployments"], and no tool
      name matches send|sign|execute|broadcast|write|configure|create
    list_deployments: every entry from the repository's manifests, filterable by
      chainId and status; every verified entry carries reportPath and
      compatibilityTestPath
    unknown tool "send_transaction" → JSON-RPC error -32602
    inspect_factory without a provider → isError with RPC_UNAVAILABLE (the CLI's
      own named code, passed through)
    inspect_factory with a provider → deepEqual against the output of
      `node tools/inspect-factory.mjs` for the same chain, deployment, block and
      caller, and the provider URL appears nowhere in the response

PASS npm run mcp:deployments:test   (no provider, FUSION_ENV_FILE=/dev/null)
  tests 4, pass 3, skipped 1 — the comparison test skips by name instead of
  failing, like the other fork-dependent tests.

PASS npm run validate:docs   (237 local links, including docs/mcp.md)

The server is stdio JSON-RPC written by hand; no dependency was added and
package-lock.json is untouched.
