T35 verification — 2026-09-09 (Europe/Warsaw)

PASS npm run vault:configure:test
  tests 3, pass 3, fail 0

  Fork scenario (anvil, Ethereum block 25937526):
    a vault is created through the unchanged deployed factory (USDC, owner
    0x2222…2222), then:
    1. substrate sDAI (asset DAI) on a USDC vault  → SUBSTRATE_ASSET_MISMATCH, exit 1
    2. owner 0x4444…4444 (no OWNER_ROLE)           → MISSING_ROLE: OWNER_ROLE, exit 1
    3. --dry-run                                    → status planned, nothing sent
    4. real run                                     → status configured,
       steps grant-roles → register-fuses → grant-substrates → set-limits,
       verification.ok true
    5. read back from the vault itself:
       getFuses() contains 0x12FD0EE1…5870 (SupplyFuseErc4626Market1)
       getMarketSubstrates(100001) contains beef01735c…64cb (Steakhouse USDC)
       getMarketLimit(100001) = 5000

  Offline refusals: unknown catalogId → UNKNOWN_INTEGRATION; market 100002 →
  MARKET_MISMATCH; --vault 0x1234 → INVALID_ARGUMENT (exit 2).

Manual exploration that established the step order and signatures (same fork):
  grantRole(100/300/200) by the owner, addFuses, addBalanceFuse,
  grantMarketSubstrates, setupMarketsLimits + activateMarketsLimits — all
  status 0x1, and the vault's price manager already prices USDC
  (getAssetPrice(USDC) = 999869740000000000, 18 decimals), so no price source had
  to be added for this integration.

No transaction was broadcast; every step ran on a local anvil fork with
impersonated operators.
