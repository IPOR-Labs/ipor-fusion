T44 verification — 2026-09-09 (Europe/Warsaw)

Every step of the trusted job was executed locally with the real provider:

  npm run agent:doctor -- --rpc --chain 1 --block 25937526
    [OK] rpc:1 chain ID 1 matches
    [OK] rpc-history:1 historical code is available at block 25937526 for 2 probe(s)

  npm run test:fork -- --chain 1 --suite deployed-factory --block 25937526
    2 suites, 2 tests passed, 0 failed

  npm run factory:inspect -- --chain 1 --deployment ethereum-fusion-factory-cd05909c
    --block 25937526 --caller 0x1111…1111  +  the manifest comparison
    "pilot factory unchanged at block 25937526"

Infrastructure failures are distinguishable, as the job requires:
  ETHEREUM_PROVIDER_URL=http://127.0.0.1:1
    [ERROR] rpc:1: RPC_UNAVAILABLE: ETHEREUM_PROVIDER_URL did not return a chain ID, exit 1
  --block 999999999
    [ERROR] rpc-history:1: HISTORICAL_STATE_UNAVAILABLE: code probe failed at block
            999999999 for 0xcd05909C…b852, 0x12FD0EE1…5870

Regression found and fixed:
  T36 added a second Ethereum suite whose state probe is the deployed ERC4626
  supply fuse. agent:doctor required exactly one probe per chain and started
  reporting RPC_CHECK_UNCONFIGURED. The doctor now requires one provider and at
  least one probe, checks every probe, and names the failing addresses.
  npm run agent:doctor:test → 5 tests, 5 passed.

Workflow shape (parsed with PyYAML):
  pilot-fork-checks.yml   jobs: [pilot-fork], trigger: workflow_call only,
                          permissions contents: read, one declared secret
  ci.yml                  jobs: [authorize, notify, build, pilot-fork, report];
                          pilot-fork needs authorize and notify, and the report's
                          success expression uses needs['pilot-fork'].result
                          (bracket form, because the job id contains a hyphen)

No organisation or repository permission was changed, and no GitHub environment
setting was touched — those remain to be confirmed by an administrator, as
docs/ci.md states.
