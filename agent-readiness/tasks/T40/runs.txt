T40 verification — 2026-09-09 (Europe/Warsaw)

PASS npm run vault:journal:test
  tests 3, pass 3, fail 0

  Scenario "a lost response never turns into a second creation" (anvil fork,
  Ethereum block 25937526):
    1. record --plan plan.json            -> prepared, nonce 0
    2. sent --id <id> --no-response       -> unknown
    3. record --plan plan.json (again)    -> DUPLICATE_IN_FLIGHT, exit 1
    4. sync --id <id> before anything was mined
         "nonce 0 has not been used (account is at 0); nothing was mined"
         "a resend is a decision for a person, not for this tool", exit 1
    5. the creation had in fact been mined; sync finds it by nonce among recent
       blocks and settles it -> confirmed, with the real hash and block, and
       prints the vault:verify command
    6. state history on disk: prepared -> unknown -> confirmed
    7. with the entry settled, record for the same plan is allowed again;
       list --json shows two entries: confirmed and prepared

  Scenario "a known hash is resolved from its receipt":
    a creation with an underlying token that has no code is mined and fails;
    sent --tx <hash> -> pending, sync -> reverted with result.status 0x0

  Arguments: an unknown command and a missing --plan exit 2; sync on a missing
  entry exits 1 with UNKNOWN_ENTRY; an artifact that is not a plan exits 1 with
  INVALID_PLAN; an empty journal prints "the journal is empty".

The journal never sends a transaction: the fork scenario sends through anvil
directly, exactly as an external signer would, and the tool only records and
resolves. Entries are written under FUSION_JOURNAL_DIR (a temporary directory in
the tests, .fusion/journal by default, git-ignored).
