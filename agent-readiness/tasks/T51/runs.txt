T51 verification — 2026-09-09 (Europe/Warsaw)

Network and identity:
  fusion_address_lookup (chain 8453, "IporFusionFactory") →
    proxy 0x1455717668fA96534f675856347A973fA907e922
    impl  0x610152A79BE7F2Aa3aA70520c9331c18fe8D33b7
  cast chain-id (BASE_PROVIDER_URL) → 8453
  cast storage <proxy> <erc1967 slot> @51000000 → …610152a79be7f2aa3aa70520c9331c18fe8d33b7
  cast block 51000000 --field hash → 0x69ab4219…4308
  getFusionFactoryVersion() @51000000 → 8   (same family as the Ethereum entry)
  runtime code @51000000: proxy 163 B 0x201c5152…39b0 (same ERC1967Proxy bytecode as
    Ethereum's proxy), impl 15395 B 0x945ad101…104b (different from Ethereum's 22 KB impl)
  getWithdrawWindowInSeconds() 86400, getVestingPeriodInSeconds() 604800,
  getPlasmaVaultBaseAddress() 0x53dEFd9d…d5d4, three DAO fee packages identical in
  values to Ethereum's

ABI: byte-identical file to the Ethereum entry's verified ABI, reused for the same
reported version. Selector probe over the ABI's 43 functions against the Base
implementation: 42 found in the runtime code; getVestingPeriodInSeconds() not found
by the byte search but returns 604800 when called — recorded as a probe limitation,
not as a missing function.

PASS FOUNDRY_PROFILE=factory_base forge test --match-path test/deployed-factories/FusionFactoryBase.t.sol
  1 passed, gas 8902876

PASS npm run test:fork -- --chain 8453 --suite deployed-factory --block 51000000
  1 suite, 1 test passed (profile factory_base, provider BASE_PROVIDER_URL)

PASS npm run test:fork -- --chain 1 --suite deployed-factory --block 25937526
  2 suites, 2 tests passed — the Ethereum selection is unaffected by the new network

PASS npm run agent:doctor -- --rpc --chain 8453 --block 51000000
  chain ID 8453 matches; historical code available at block 51000000 for 1 probe

PASS npm run validate:deployments   (3 manifests: 2 production + fixture)
PASS npm run validate:test-suites   (12 suites classified)
PASS npm run validate:docs          (235 local links)

Compilation settings were not changed: [profile.factory_base] only sets ffi = false
and empty fs_permissions, exactly like [profile.factory_ethereum], and inherits the
default compiler, optimizer and EVM version. The suite schema's profile enum gained
"factory_base".

No address or token is shared between the networks; the test asserts
BASE_FACTORY != ETHEREUM_FACTORY so that a copy-paste would fail.
