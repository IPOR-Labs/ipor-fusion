T49 verification — 2026-09-09 (Europe/Warsaw)

Address and interface, from sources rather than assumption:
  fusion_address_lookup (chain 1, "Erc4626") →
    ERC4626PriceFeedFactoryProxy 0xf58Fcce9370aBa552032d3EA47baA486F70c0FdC
    ERC4626PriceFeedFactoryImpl  0xe08AfF4910Fb61AcC2EacB03b0a6132B01D1aa61
  cast storage <proxy> <erc1967 slot> @25937526 → …e08aff4910fb61acc2eacb03b0a6132b01d1aa61
    (the implementation the lookup names is the one the proxy points at)
  contract_source(proxy) → ERC1967Proxy, solc v0.8.26, optimizer 1000000, cancun
  contract_abi(implementation) → ERC4626PriceFeedFactory, 30 entries, create(address,address) 0x3e68680a
  selector probe on the implementation's runtime code: PRESENT 0x3e68680a, 0x8da5cb5b, 0xc4d66de8
  cast call <proxy> owner() → 0xa9622a0f359EFf9c253F0Be861C480B03e610392
  runtime code hashes @25937526:
    proxy          0xa76a49b5…2742 (176 bytes)
    implementation 0x3623e0eb…86d7 (7147 bytes)

PASS npm run test:fork -- --chain 1 --suite deployed-price-feed-factory --block 25937526
  Erc4626PriceFeedFactoryEthereumTest: 2 passed, 0 failed
    testShouldCreateAWorkingFeedThroughTheDeployedFactory (gas 1378798)
      feed created by a permissionless caller through the unchanged proxy
      feed.vault() == Steakhouse USDC, decimals() == 18 (WAD)
      latestRoundData(), read as the middleware, is positive and equals
        convertToAssets(1e18) * USDC price / 10^(6+18) * 1e18 to within 1 wei,
        and lies between 0.5e18 and 5e18 as a USDC vault share must
    testShouldRejectAVaultThatIsNotErc4626 — create(USDC, middleware) reverts

PASS npm run validate:test-suites      (10 suites classified)
PASS npm run validate:deployments      (the new entry is verified with full proof)
PASS npm run validate:docs             (231 local links)

Recorded as unknown rather than guessed:
  the factory exposes no version getter        → interface.reportedVersion: null
  the implementation's compiler metadata       → code.compiler: null, with a note
                                                 in provenance.json
  deployment transaction and block             → null, as for the first entry

The test uses the factory, the middleware and the ERC4626 vault unchanged: no
upgrade, no etch, no role grant, no funding, no broadcast.
