T36 verification — 2026-09-09 (Europe/Warsaw)

PASS npm run test:fork -- --chain 1 --suite deployed-factory --block 25937526
  FusionFactoryEthereumDeployedUsageTest          1 passed (T25)
  Erc4626StrategyLifecycleEthereumTest            1 passed, gas 2864956
  2 suites, 2 tests passed, 0 failed

The lifecycle test asserts, in order:
  deposit 100 000 USDC     shares minted, totalAssets ≈ 100 000 (±1 USDC),
                           the vault holds the full deposit idle
  supply 40 000 USDC       Steakhouse USDC shares increase, idle balance is
                           exactly 60 000 USDC, totalAssets unchanged (±1 USDC)
  exit 40 000 USDC         idle balance back to ≈ 100 000 USDC (±1 USDC)
  redeem all shares        assets out ≈ 100 000 USDC (±1 USDC), depositor whole,
                           zero shares left

Two failures on the way, both real defects the test caught:

1. MarketLimitExceeded(100001, 59999999999, 0)
   setupMarketsLimits takes a WAD fraction (1e18 = 100%), not basis points.
   T35's input field limitInPercentageBps: 5000 therefore configured a limit of
   effectively zero. Renamed to limitInPercentageWad (decimal string, because
   5e17 does not fit a JSON number safely) in the schema, the example, the tool,
   its test and docs/vaults.md.

2. FailedInnerCall() on the supply action
   The deployed fuse 0x12FD0EE1…5870 does not expose this checkout's
   enter((address,uint256,uint256)) 0x41b11ae7. Probing its runtime code:
     PRESENT 0xd5ee7916 enter((address,uint256))
     PRESENT 0x92876ac8 exit((address,uint256))
     absent  0x41b11ae7 / 0x8467bd30
   The test now encodes the deployed signatures, the catalog's actionFuse gets
   matchesCurrentSource: false plus a note naming both selectors, and
   docs/fuse-catalog.md explains why interface and deployments are separate.

PASS npm run validate:test-suites      (9 suites classified)
PASS npm run validate:catalog          and npm run catalog:check
PASS npm run vault:configure:test      (3 tests, with the WAD limit read back)

Test-only shortcuts, marked in the test source and in the suite catalog: `deal`
for the depositor's USDC and one `vm.warp` past the vault's own redemption delay.
The deployed factory and both deployed fuses are used unchanged — no upgrade, no
etch, no role granted on them.
