T38 verification — 2026-09-09 (Europe/Warsaw)

Every symptom in the document comes from a run recorded in this plan:
  RPC_UNAVAILABLE / CHAIN_MISMATCH / HISTORICAL_STATE_UNAVAILABLE   T18, T28 tests
  IMPLEMENTATION_MISMATCH                                            T24 (block 23831825)
  UNSUPPORTED_FACTORY_VERSION                                        T28 test (version 9)
  FailedInnerCall() from a fuse delegatecall                         T36 (deployed fuse ABI)
  MISSING_ROLE                                                       T35 test
  FEE_PACKAGE_MISMATCH (values and list)                             T28 tests
  SUBSTRATE_ASSET_MISMATCH                                           T35 test (sDAI on a USDC vault)
  MarketLimitExceeded(100001, 59999999999, 49999999999)              T36
  PRICE_SOURCE_MISSING                                               T35 tool check
  TRANSACTION_UNKNOWN / pending / reverted / FOREIGN_CREATION_EVENT  T31 tests
  SIMULATION_INCONSISTENT                                            T29 guard

Diagnostic commands executed while writing the page:

  cast chain-id                                          -> 1
  cast block 25937526 --field hash                       -> 0x6b7043c5...d719
  cast code --block 25937526 <factory> | head -c 20      -> 0x6080604052600a600c
  cast storage <factory> <erc1967 slot> at two blocks:
      23831825 -> 0x...d48d9528581b6bbc5e259f1e3720619bb55d5e0d
      25937526 -> 0x...f19c1e9f6616f6056af1e322a86fdaaaaf0263f5   (the upgrade is real)
  cast call <factory> getFusionFactoryVersion()          -> 8, equal to the manifest
  selector probe on the deployed supply fuse             -> PRESENT 0xd5ee7916 enter((address,uint256))
  cast call <accessManager> hasRole(...) for 1/100/200/300 on a configured fork vault
      -> owner holds 1, 100 and 300 and not 200 (the alpha is a different address)
  cast call <vault> getMarketSubstrates(100001)          -> the granted Steakhouse USDC entry
  cast call <vault> getMarketLimit(100001)               -> 500000000000000000
  cast call <priceManager> getAssetPrice(USDC)           -> 999869740000000000, 18
  cast call <priceManager> getSourceOfAssetPrice(USDC)   -> 0x000...000 while the price answers,
      which is why the page warns that a zero source is not proof of an unpriceable asset
  cast call <erc4626 vault> asset() and <plasma vault> asset() -> both USDC
  cast nonce <sender> and --block pending                -> 1 and 1
  cast run --help                                        -> available

The fork used for the vault-side commands was a local anvil at block 25937526
with a vault created and configured by the repository's own tools; it was stopped
afterwards. No transaction was broadcast.
