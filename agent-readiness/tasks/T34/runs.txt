T34 verification — 2026-09-09 (Europe/Warsaw)

PASS npm run catalog:generate
  updated: catalog/fuses.json (1 integration(s))
  interface.generated now holds:
    marketConstantSource contracts/libraries/IporFusionMarkets.sol:358
    sources sha256 for Erc4626SupplyFuse.sol and Erc4626BalanceFuse.sol
    enter  Erc4626SupplyFuseEnterData → enter((address,uint256,uint256)) 0x41b11ae7
           fields at Erc4626SupplyFuse.sol:18, :20, :22 (struct at :16)
    exit   Erc4626SupplyFuseExitData  → exit((address,uint256,uint256))  0x8467bd30
           fields at Erc4626SupplyFuse.sol:28, :30, :32 (struct at :26)

PASS npm run catalog:check
  catalog is up to date with the sources (1 integration(s))

PASS npm run validate:catalog          (also cross-checks described vs generated)
PASS npm run validate:catalog:test     (13 tests)

PASS npm run catalog:generate:test
  tests 6, pass 6, fail 0
    committed catalog is up to date
    regenerating twice is byte-identical ("unchanged")
    adding `address recipient;` to the enter struct changes the signature to
      enter((address,uint256,uint256,address)) and the selector, and
      validate:catalog then reports the described field list as wrong
    a hand-edited selector makes catalog:check exit 1 with OUT_OF_DATE
    edited meanings, substrate text, notes, roles, valuation and tests survive
      regeneration unchanged
    a renamed struct exits 2 with STRUCT_NOT_FOUND

No network access and no contract build were needed; the generator reads sources
and calls `cast sig`.
